# Product backlog

Priority **0** is highest (do next). **5** is lowest.

Brand page sectioning (month selector with results; SKU tools with the editor; reward controls with thresholds) shipped in [PR #3](https://github.com/rickrangel-mer/m-rewards-simulator/pull/3). FastAPI + Jinja2; no Streamlit. Web never talks to Athena.

| P | Item | Notes |
|---|---|---|
| **0** | **Persist proposed points, reward names, and thresholds** | **Next.** Today edits live only in a 12-hour browser cookie (`SessionMiddleware` in [`app.py`](app.py)). Railway redeploy, a new browser, or a coworker opening the site starts from Excel/`BRAND_DEFAULTS` again. Store SKU proposed points **and** reward name/threshold lists in Railway Postgres, shared across users, so “Update Simulation” / import / bulk apply / add-remove reward survive refresh. Excel workbooks stay the published current-points source. Do not reintroduce Streamlit. |
| **1** | **Review store-inclusion logic (Athena + simulator)** | Rick’s concern: stores without “enough” purchases may be dropped. There is **no** `HAVING` / min-quantity filter today. What *does* drop stores: Athena [`fetch_order_data()`](data.py) only returns stores that ordered a SKU on a brand Excel list; [`get_month_orders()`](simulator.py) / [`simulate()`](simulator.py) only score stores with ≥1 of those SKUs in the selected month; “% of stores” is that population, not all Mercaso stores; store-detail table is top 500; histogram clips the top 1% **for chart bins only**. Review whether the denominator should be all stores, ever-ordered-this-brand, or current-month orderers. Also check `SUM(li.initial_quantity)` vs net quantity and cancelled orders. Investigate first; change the query only if the product rule is wrong. |
| **2** | **Per-brand ambient color** | One palette today (`--accent: #0f6a5a` in [`static/styles.css`](static/styles.css)). Coca-Cola / Monster / Ferrera pages should feel different (header, buttons, metric cards, histogram bars) without a branding overhaul. **Blocked on Rick** providing an example scheme per brand. Implementation sketch: `data-brand` on `<body>` + CSS variables; keep layout/sectioning. New brands (P3) should pick or inherit a theme. |
| **3** | **Create a new brand page from a template** | Adding a fourth rewards list today means a custom Excel layout, a new loader in [`data.py`](data.py), an entry in `BRAND_DEFAULTS` ([`simulator.py`](simulator.py)), and a `load_brand_skus` branch ([`app.py`](app.py)). Ship a single upload contract and an in-app “new brand” flow instead of code changes per brand. **Required upload:** (1) display name + URL slug; (2) CSV/Excel with `sku`, `product_title`, `current_points` (extra columns optional); (3) at least one reward name + point cutoff. Optional: accent colors once P2 palettes exist. Unify the three existing loaders onto that schema; nav should come from the registry, not hardcoded links. Persist config in Postgres (depends on P0). Athena refresh already unions all loaded SKUs — new SKUs need a refresh (or wait for the 1st-of-month cron) before simulations have orders. |
