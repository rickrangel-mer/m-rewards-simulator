# Product backlog

Priority **0** is highest (do next). **5** is lowest.

Brand page sectioning (month selector with results; SKU tools with the editor; reward controls with thresholds) shipped in [PR #3](https://github.com/rickrangel-mer/m-rewards-simulator/pull/3). FastAPI + Jinja2; no Streamlit. Web never talks to Athena.

**Direction:** Excel stays the *bulk edit format* (upload/download). It should not stay the *live config*. Postgres should own brand SKUs, points, and rewards; the three git workbooks (`M-rewards-cocacola.xlsx`, `M-rewards-monster.xlsx`, `M-rewards-ferrera.xlsx`) become a one-time seed, then leave runtime.

| P | Item | Notes |
|---|---|---|
| **0** | **Persist proposed points, reward names, and thresholds** | **Next.** Today edits live only in a 12-hour browser cookie (`SessionMiddleware` in [`app.py`](app.py)). Railway redeploy, a new browser, or a coworker opening the site starts from Excel/`BRAND_DEFAULTS` again. Write SKU proposed points **and** reward name/threshold lists to Railway Postgres, shared across users, so in-page edit / import / bulk apply / add-remove reward survive refresh. First slice of the Postgres catalog (P1). Do not reintroduce Streamlit. |
| **1** | **Replace git Excel files with web-managed catalogs** | Same three files are still read at runtime by the website and by `refresh_orders.py` (`load_all_skus()` in [`data.py`](data.py)). Each brand has a different sheet/column layout, so they are not interchangeable. **Target:** download current catalog as Excel, upload Excel to replace or merge (preview before save), store in Postgres, point the web app **and** the Athena refresh at that store, then stop shipping the xlsx files as live config. Keep Excel as the management UX — do not build a from-scratch SKU CMS. Canonical upload columns: `sku`, `product_title`, `current_points` (extra columns optional). Seed once from the existing workbooks. One source of truth; no “git Excel vs uploaded Excel” split. |
| **2** | **Review store-inclusion logic (Athena + simulator)** | Rick’s concern: stores without “enough” purchases may be dropped. There is **no** `HAVING` / min-quantity filter today. What *does* drop stores: Athena [`fetch_order_data()`](data.py) only returns stores that ordered a SKU on the brand list; [`get_month_orders()`](simulator.py) / [`simulate()`](simulator.py) only score stores with ≥1 of those SKUs in the selected month; “% of stores” is that population, not all Mercaso stores; store-detail table is top 500; histogram clips the top 1% **for chart bins only**. Review whether the denominator should be all stores, ever-ordered-this-brand, or current-month orderers. Also check `SUM(li.initial_quantity)` vs net quantity and cancelled orders. Investigate first; change the query only if the product rule is wrong. After P1 the SKU list comes from Postgres, not the git files — same inclusion rule either way. |
| **3** | **Per-brand ambient color** | One palette today (`--accent: #0f6a5a` in [`static/styles.css`](static/styles.css)). Coca-Cola / Monster / Ferrera pages should feel different (header, buttons, metric cards, histogram bars) without a branding overhaul. **Blocked on Rick** providing an example scheme per brand. Implementation sketch: `data-brand` on `<body>` + CSS variables; keep layout/sectioning. New brands (P4) should pick or inherit a theme. |
| **4** | **Create a new brand page from a template** | Depends on P1. Adding a fourth list today means a custom Excel layout, a new loader in [`data.py`](data.py), an entry in `BRAND_DEFAULTS` ([`simulator.py`](simulator.py)), and a `load_brand_skus` branch ([`app.py`](app.py)). After catalogs are web-managed, “new brand” is: display name + URL slug, upload the canonical Excel, at least one reward name + cutoff. Nav from the Postgres registry. Optional accent once P3 palettes exist. New SKUs only appear in simulations after an Athena refresh (or the 1st-of-month cron). |
